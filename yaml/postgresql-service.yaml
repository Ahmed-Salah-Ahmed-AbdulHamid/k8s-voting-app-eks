apiVersion : v1 
kind: Service 
metadata: 
  name: db
  annotations: 
    service.beta.kubernetes.io/aws-load-balancer-scheme: "internet-facing"
spec:
  type: ClusterIP  
  selector:
       app: db
  ports:
  - protocol: TCP
    port: 5432
    targetPort: 5432
